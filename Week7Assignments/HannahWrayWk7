#! /usr/bin/env python
# variables that store floating point numbers
number1=float(8.5)
number2=float(4.5)
# variable that stores the floating point number sum of number1+number2
sum=float(number1+number2)
# converts the float variables to strings
str1=str(number1)
str2=str(number2)
strS=str(sum)
# concatenates strings to describe mathematical process
sentence="This script added "+str1+" to "+str2+" and got "+strS
# prints sentence describing mathematical process
print(sentence)
